# api: a class which has methods corresponding to the business operations of the tracker
#       ie. makeNewUser()
# The api will create or receive an instance of the persistence layer (db) and delegate operations to it...

class Api:
    #constructor? ???

    def createUser(self):
        print("create user was called")
        return
